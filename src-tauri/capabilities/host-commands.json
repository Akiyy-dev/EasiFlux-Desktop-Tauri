{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "host-commands",
  "description": "Existing application commands for the local main webview only",
  "local": true,
  "webviews": ["main"],
  "permissions": ["allow-host-commands"]
}
